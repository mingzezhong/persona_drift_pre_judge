# Scheduler jobs

There are no active v2 PBS jobs yet. Job files will be added only after the
protocol gates for model revisions, corpus manifests, persona rubrics, pressure
templates, generation settings, artifact schema, and resume behavior pass.

The first executable job will be a small two-GPU smoke/preflight, not a
dose-finding or main scientific run.
